# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

This is an npm workspaces monorepo:

- `apps/homepage` — Beyond180 Sports landing page
- `apps/sports-analytics` — Sports analytics app
- `packages/shared` — shared Supabase client, DB types, and data helpers

Configure Metro watch folders for workspace packages when adding new apps.
